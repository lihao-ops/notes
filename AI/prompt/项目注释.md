**请严格按照以下要求为我生成 Java 代码：**

### **① 类级注释（Class-Level Comment）**

在每个 class 文件顶部添加 **中英双语说明注释**，内容必须包括：

- 类的功能说明（What this class does）
- 使用场景（When / Why used）
- 设计目的（Design purpose）
- 涉及的核心组件说明（Core components）

注释格式如下（必须遵守）：

```
/**
 * 类说明 / Class Description:
 * 中文：……
 * English: ……
 *
 * 使用场景 / Use Cases:
 * 中文：……
 * English: ……
 *
 * 设计目的 / Design Purpose:
 * 中文：……
 * English: ……
 */
```

------

### **② 方法级注释（Method-Level Comment）**

在每个方法上方编写 **详细的中英双语注释**，包含：

- 方法用途
- 参数说明
- 返回值说明
- 异常情况说明
- 逻辑概述（高层级）

格式如下（必须遵守）：

```
/**
 * 方法说明 / Method Description:
 * 中文：……
 * English: ……
 *
 * 参数 / Parameters:
 * @param x 中文说明 / English description
 *
 * 返回值 / Return:
 * 中文说明 / English description
 *
 * 异常 / Exceptions:
 * 可能抛出的异常中文/英文说明
 */
```

------

### **③ 代码行级注释（Line-Level Comment）**

对于每一段关键逻辑（核心代码），必须在上一行添加说明性注释（中英双语），格式如下：

```
// 中文：这里通过 … 实现 …
// English: Here we … to achieve …
核心代码;
```

要求：

- 所有关键逻辑必须有注释
- 非核心（如 getter/setter）可以省略
- 注释必须比核心代码更“解释本质”，不能只写“处理逻辑”这种废话

------

### **④ 测试类规范要求（Test Class Requirements）**

所有 `*Test.java` 测试类必须包含：

#### **1. 类级头注释（测试目的）**

```
/**
 * 测试目的 / Test Purpose:
 * 中文：……
 * English: ……
 *
 * 预期结果 / Expected Result:
 * 中文：……
 * English: ……
 *
 * 执行方式 / How to Execute:
 * 中文：……
 * English: ……
 */
```

#### **2. 每个测试方法必须有：**

- 测试目标说明（What is tested）
- 输入/输出说明
- 预期结果说明

------

### **⑤ 提升代码质量整体要求**

你生成的代码必须遵循：

- **阿里巴巴 Java 开发手册思想（结构清晰）**
- **JavaDoc 标准**
- **中英文注释等距对齐，整齐美观**
- **变量命名专业、清晰、可读，不要随意缩写**
- **每个逻辑块前必须有小段说明注释**

------

### **⑥ 禁止输出**

模型必须避免以下错误：

🚫 不能省略任何注释
 🚫 不能用 “省略…” 或 “略…”
 🚫 不能输出不完整伪代码
 🚫 不能只写中文或只写英文
 🚫 不能产生未经过解释的魔法数字
 🚫 不能忽略异常处理

------

### **⑦ 输出结构要求**

输出代码格式必须严格使用：

```
​```java
// your code here
​```
```

------

# 🔥 **最终指令：**

> **请使用我以上给出的规范，生成符合双语注释 + 类说明 + 方法说明 + 行级说明 + 测试类规范 的完整 Java 代码。**

类注释必须在import下，在public class上，且必须包含这个类的实现思路，与其它类是否有嵌套关系，是什么样的！！！核心代码必须在上一行行注释！！！